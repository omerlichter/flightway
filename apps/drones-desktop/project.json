{
  "name": "drones-desktop",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/drones-desktop/src",
  "projectType": "application",
  "tags": ["api"],
  "targets": {
    "build": {
      "executor": "nx-electron:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/drones-desktop",
        "main": "apps/drones-desktop/src/main.ts",
        "tsConfig": "apps/drones-desktop/tsconfig.app.json",
        "assets": ["apps/drones-desktop/src/assets"]
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false,
          "fileReplacements": [
            {
              "replace": "apps/drones-desktop/src/environments/environment.ts",
              "with": "apps/drones-desktop/src/environments/environment.prod.ts"
            }
          ]
        }
      }
    },
    "serve": {
      "executor": "nx-electron:execute",
      "options": {
        "buildTarget": "drones-desktop:build"
      }
    },
    "package": {
      "executor": "nx-electron:package",
      "options": {
        "name": "drones-desktop",
        "frontendProject": "drones-client",
        "sourcePath": "dist/apps",
        "outputPath": "dist/packages",
        "prepackageOnly": true
      }
    },
    "make": {
      "executor": "nx-electron:make",
      "options": {
        "name": "drones-desktop",
        "frontendProject": "drones-client",
        "sourcePath": "dist/apps",
        "outputPath": "dist/executables",
        "win": {
          "icon": "apps\\drones-client\\src\\favicon.ico"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["apps/drones-desktop/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/drones-desktop/jest.config.ts"
      }
    }
  }
}
